// 설정 파일
// 전역으로 사용되는 설정값 지정

const config = {
    // 공통 스타일 지정
    styles: {
        // 공통 레이아웃 스타일 지정
        layout: {
            // 사이드바 최대 넓이
            maxDrawerWidth: '250px',

            // 상단바 최대 높이
            appBarHeight: '56px',

            // 콘텐츠 영역 최대 넓이
            maxContentWidth: '1200px',
        }
    }
}

export default config;