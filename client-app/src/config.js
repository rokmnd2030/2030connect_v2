const config = {
    styles: {
        layout: {
            maxDrawerWidth: '250px',
            appBarHeight: '56px',
            maxContentWidth: '1200px',
        }
    }
}

export default config;